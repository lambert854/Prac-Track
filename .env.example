# Database Configuration
# For local development, you can use your Neon dev branch or a local PostgreSQL instance
# For production, use your Neon production connection string
DATABASE_URL="postgresql://username:password@host:5432/database?sslmode=require"

# NextAuth Configuration
# NEXTAUTH_URL should match your domain
# Local: http://localhost:3000
# Production: https://prac-track.com
NEXTAUTH_URL="http://localhost:3000"

# Generate a secure secret with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-minimum-32-characters-long"

# Email Service (Optional - Resend API)
# Sign up at https://resend.com to get an API key
# Without this, emails will be logged to console
RESEND_API_KEY="re_your_api_key_here"

# Note: Copy this file to .env.local for local development
# Never commit .env.local to version control
